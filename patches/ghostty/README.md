# Ghostty patches

The pinned Ghostty revision is consumed as a clean checkout by native and WASM builds.

No local patches are currently applied. OSC color-query responses, terminal snapshots,
and terminal compression are available through upstream `libghostty-vt`.
