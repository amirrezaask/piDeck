# Production Readiness Implementation Plans

The terminal roadmap lives in [`plans/terminal/README.md`](./terminal/README.md).
Keep its dependency order, STOP conditions, verification gates, and status rows
in sync with terminal work in this repository.

Generated by the improve skill on 2026-08-26 against commit `e1a8022` and the current uncommitted working tree. The working tree was already dirty when the audit began; executors must preserve unrelated changes and inspect `git diff` before editing.

Execute in the order below unless dependencies say otherwise. Each executor must read its plan fully, honor STOP conditions, run every verification gate, and update its status row.

## Execution order and status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---:|---:|---|---|
| 001 | Make accepted-run admission durable and compensating | P1 | M | — | DONE |
| 002 | Stop paid execution when durable event persistence fails | P1 | M | 001 | DONE |
| 003 | Persist and recover Pi session ownership | P1 | L | 001, 002 | DONE |
| 004 | Make renderer interventions idempotent | P1 | M | 001 | DONE |
| 005 | Make event-stream failure and reconnect semantics reliable | P1 | M | — | DONE |
| 006 | Preserve run visibility during partial server failure | P1 | M | 005 | DONE |
| 007 | Load all active and historical runs safely | P1 | M | 006 | DONE |
| 008 | Enforce configured authentication on standalone loopback requests | P1 | S | — | DONE |
| 009 | Require TLS for token-bearing non-loopback supervisors | P1 | S | 008 | DONE |
| 010 | Make migration 013 rollback/reapply safe | P1 | M | — | DONE |
| 011 | Bound supervisor and renderer event memory | P1 | L | 002, 005 | DONE |
| 012 | Add crash, restart, and disk-write-failure integration tests | P1 | L | 001, 002, 003, 010 | DONE |
| 013 | Add packaged Electron smoke tests | P1 | M | 003, 008, 009, 012 | DONE |
| 014 | Add a sustained event-stream soak test | P1 | M | 005, 011, 012 | DONE |
| 015 | Enforce production CI and release gates | P1 | M | 010, 012, 013, 014 | DONE |
| 016 | Document and verify backup/restore procedures | P1 | M | 003, 010, 012 | DONE |

Status values: `TODO`, `IN PROGRESS`, `DONE`, `BLOCKED: <reason>`, `REJECTED: <reason>`.

## Dependency notes

- Plans 001–003 establish the run/session durability model before crash testing codifies it.
- Plan 004 reuses the receipt/idempotency machinery whose admission behavior is stabilized by plan 001.
- Plans 005–007 fix client truthfulness before pagination and degraded snapshots expand state handling.
- Plan 011 relies on plan 002's persistence-failure policy and plan 005's reconnect contract.
- Plan 012 is the destructive/failure-injection validation layer for plans 001–003 and migration 013.
- Plans 013 and 014 must be green before plan 015 makes them required release gates.
- Plan 016 documents the final storage/session layout after plan 003 and validates migration assumptions after plan 010.

## Coverage matrix: all 16 requested items

| Requested item | Covered by |
|---|---|
| 1. Pi sessions cannot resume after restart | 003 |
| 2. Accepted paid work can remain queued | 001 |
| 3. Event persistence failures are swallowed | 002 |
| 4. Follow-up/steer are not retry-safe | 004 |
| 5. Streams can silently stop/conceal failures | 005 |
| 6. Auxiliary failure hides a server | 006 |
| 7. Only first 100 runs load | 007 |
| 8. Standalone loopback bypasses token | 008 |
| 9. Remote tokens may use plaintext HTTP | 009 |
| 10. Migration 013 rollback is unsafe | 010 |
| 11. Event/transcript memory is unbounded | 011 |
| 12. Real crash/restart and disk-write-failure tests | 012 |
| 13. Electron packaged smoke test | 013 |
| 14. Enforced CI release gates | 015 |
| 15. Backup/restore documentation | 016 |
| 16. Long-running stream soak test | 014 |

## Repository-wide verification baseline

Run after every plan unless the plan narrows an intermediate step:

```sh
pnpm check
pnpm test
pnpm lint
pnpm format:check
```

Before release, all must also pass:

```sh
pnpm test:coverage
pnpm test:e2e
pnpm build
pnpm audit --prod --audit-level high
```

At planning time, `check`, unit tests, lint, format, build, and audit passed. `test:e2e` had one mobile project-picker timeout and `test:coverage` failed the configured 80% threshold. Plan 015 must not waive either failure.

## Findings considered and rejected

- None. The operator explicitly selected all 16 items.
