# Agent client

`@pideck/agents-client` contains the agent surface used by `apps/web` and the legacy Electron shell.

Public entry points:

- `@pideck/agents-client/client`
- `@pideck/agents-client/styles.css`
- `@pideck/agents-client/soak` for the repository soak harness
